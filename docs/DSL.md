# DSL Reference

Этот документ описывает текущий DSL для эффектов в `mylamp`. Он специально сделан маленьким: язык должен быть понятным человеку, предсказуемым для LLM и достаточно жёстким для последующей реализации на ESP32-C3.

## Зачем он нужен

DSL нужен, чтобы:

- быстро собирать визуальные эффекты без правки firmware на C++;
- держать контракт между frontend, mock backend и device-side runtime;
- ограничивать синтаксис, чтобы генерация через LLM не расползалась в выдуманный язык.

## Базовые сущности

DSL v1 опирается на три конструкции:

- `effect`
- `sprite`
- `layer`

## `effect`

Каждый файл эффекта начинается с имени:

```txt
effect "rainbow"
```

Это имя:

- показывается в UI;
- участвует в сохранении preset-а;
- служит главным идентификатором эффекта.

## `sprite`

`sprite` описывает форму через bitmap-маску.

Пример:

```txt
sprite heart {
	bitmap """
	.##.##.
	#######
	#######
	.#####.
	..###..
	...#...
	"""
}
```

Смысл символов:

- `#` — закрашенный пиксель
- `.` — пустой пиксель

## `layer`

`layer` выбирает sprite и задаёт его поведение.

Пример:

```txt
layer love {
	use heart
	color rgb(255, 40, 80)
	x = 10 + sin(t) * 4
	y = 4 + cos(t * 1.2) * 2
	scale = 1
	rotation = t * 0.5
	blend = add
	visible = 1
}
```

Поддерживаемые поля слоя v1:

- `use`
- `x`
- `y`
- `scale`
- `rotation`
- `blend`
- `color`
- `visible`

`rotation` задаётся в радианах и вращает sprite вокруг центра его bitmap.
`blend` задаёт режим наложения слоя: `normal`, `add`, `multiply`.

## Доступные переменные

- `t` — время в секундах с момента старта эффекта
- `dt` — дельта времени между кадрами
- `x`, `y` — текущие координаты пикселя
- `nx`, `ny` — нормализованные координаты от `0` до `1`

## Доступные функции

Математика:

- `sin(value)`
- `cos(value)`
- `abs(value)`
- `min(a, b)`
- `max(a, b)`
- `clamp(v, a, b)`

Сенсоры:

- `temp()`
- `humidity()`

Цвет:

- `rgb(r, g, b)`
- `hsv(h, s, v)`

## Минимальный шаблон эффекта

```txt
effect "my_effect"

sprite dot {
	bitmap """
	#
	"""
}

layer paint {
	use dot
	color rgb(255, 120, 80)
	x = 10
	y = 6
	scale = 2
	rotation = 0
	blend = normal
	visible = 1
}
```

## Живой пример

```txt
effect "heart"

sprite heart {
	bitmap """
	.##.##.
	#######
	#######
	.#####.
	..###..
	...#...
	"""
}

layer love {
	use heart
	color rgb(255, 30 + 20 * sin(t * 5), 80)
	x = 10 + sin(t) * 4
	y = 4 + cos(t * 1.2) * 2
	scale = 1 + abs(sin(t * 2))
	rotation = t * 0.8
	blend = add
	visible = 1
}
```

## Что в v1 не поддерживается

Этот DSL пока не является полноценным языком программирования. Вне текущего scope:

- циклы;
- рекурсия;
- пользовательские функции;
- произвольные скрипты;
- collision logic;
- любые новые ключевые слова, которых нет в этом документе.

## Как писать эффекты без боли

Практический путь:

1. Начинай с одного `sprite`.
2. Добавляй один `layer`.
3. Сначала ставь фиксированные `x`, `y`, `scale`.
4. Только потом добавляй `sin`, `cos` и цветовую анимацию.
5. После этого проверяй и сохраняй preset.

Если сразу пытаться написать «огонь, молнию, радугу, шум и часы в одном файле», дебажить такой код потом бессмысленно.

## Рекомендации для LLM

LLM лучше работает, если задача поставлена жёстко. В запросе полезно явно фиксировать:

- размер полотна: `32x16`;
- желаемый образ эффекта;
- список разрешённых сущностей и функций;
- запрет на новые ключевые слова;
- требование вернуть только DSL-код без пояснений.

Отдельный copy-paste prompt лежит здесь:

- [docs/LLM_EFFECT_PROMPT.md](LLM_EFFECT_PROMPT.md)
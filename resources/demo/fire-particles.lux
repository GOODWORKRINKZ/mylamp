effect "fire_particles"

sprite dot {
  bitmap """
  #
  """
}

for j = 0; j < 12; j = j + 1 {
  layer p {
    use dot
    color hsv(15 + j * 2, 1, 1 - j * 0.08)
    x = sin(j * 2.7 + t) * 3 + 8
    y = (t * 3 + j * 2) % 32
    scale = 1 + (j % 2)
    blend = add
    visible = 1
  }
}

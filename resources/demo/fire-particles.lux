effect "fire_particles"

sprite dot {
  bitmap """
  #
  """
}

for i = 0; i < 12; i = i + 1 {
  layer p {
    use dot
    color hsv(15 + i * 2, 1, 1 - i * 0.08)
    x = sin(i * 2.7 + t) * 3 + 8
    y = (t * 3 + i * 2) % 32
    scale = 1 + (i % 2)
    blend = add
    visible = 1
  }
}

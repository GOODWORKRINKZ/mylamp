effect "snake"

sprite dot {
  bitmap """
  #
  """
}

for i = 0; i < 10; i = i + 1 {
  layer seg {
    use dot
    color hsv(i * 30, 1, 1)
    x = sin(t * 2 + i * 0.6) * 12 + 16
    y = cos(t * 2 + i * 0.6) * 6 + 8
    scale = 1 + (i % 2)
    visible = 1
  }
}

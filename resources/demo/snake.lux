effect "snake"

sprite dot {
  bitmap """
  #
  """
}

for j = 0; j < 10; j = j + 1 {
  layer seg {
    use dot
    color hsv(j * 30, 1, 1)
    x = sin(t * 2 + j * 0.6) * 12 + 16
    y = cos(t * 2 + j * 0.6) * 6 + 8
    scale = 1 + (j % 2)
    visible = 1
  }
}
